streamlit>=1.31.1
pandas>=2.2.0
numpy>=1.26.4
scikit-learn>=1.4.1.post1
# NOTE: On GCP VMs / Docker use the CPU wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# The plain "torch" package pulls CUDA libraries (very large).
torch>=2.2.1
yfinance>=0.2.36
requests>=2.31.0
crewai>=0.51.0
litellm>=1.44.10
python-dotenv>=1.0.0   # optional, for local .env loading if you want it later
