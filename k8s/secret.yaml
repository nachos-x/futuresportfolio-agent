# Create this secret with:
# kubectl create secret generic openrouter-secret \
#   --from-literal=api-key=sk-or-YOUR_ACTUAL_KEY
#
# DO NOT commit real values to git.

apiVersion: v1
kind: Secret
metadata:
  name: openrouter-secret
type: Opaque
stringData:
  api-key: "REPLACE_ME"